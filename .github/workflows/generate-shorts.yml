name: Generate Shorts V3

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test workflow
        run: |
          echo "======================"
          echo "Shorts Factory V3"
          echo "Workflow aktif"
          echo "======================"
