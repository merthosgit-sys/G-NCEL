name: Generate Shorts V3

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: |
          echo "Shorts Factory V3 başladı"
          echo "Cloud runner hazır"
